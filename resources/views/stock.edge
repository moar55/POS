<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Stock Page</title>
    @include('includes.include-css')
    {{ css('style') }}
  </head>
  <body>
    <div class="logout">
      <a type="button" class="logout-button" href="/logout">Logout</a>
    </div>
    <section class="title">
      <h1>Stock</h1>
    </section>

<div class="container">
<br>

	<br/><br/>

	<table class="table table-responsive table-striped" style="margin-top:-3%;">
		<thead>
			<tr>
				<th>R.</th>
				<th>Quantity</th>
				<th>Price</th>
        <th>Manufacturer</th>
        <th>edit</th>
        <th>delete</th>
			</tr>
		</thead>
		<tbody>
      @each(item in items)
    		<tr>
            <td>
              <a id="{{item.R}}" class="itemR" data-toggle="modal" data-target="#myModal">
                {{ item.R }}
              </a>  
            </td>
            <td> {{ item.quantity }} </td>
            <td> {{ item.price }} </td>
            <td> {{ item.manufacturer }} </td>
            <td>
              <a class="btn btn-info">
                <i class="glyphicon glyphicon-pencil"></i>
              </a>
            </td>
            <td>
              <button class="btn btn-danger">
            	   <i class="glyphicon glyphicon-trash"></i>
              </button>
            </td>
    		</tr>
        @else
          <h2>No Items in Stock </h2>
      @endeach
		</tbody>
	</table>
{{--  modals   --}}
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="myModalLabel">{{ item.R }}</h4>
      </div>
      <div class="modal-body">
        TODO: Insert the table here honey..
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button>
      </div>
    </div>
  </div>
</div>
{{--  end modals  --}}

  @include('includes.include-js')
  </body>
</html>
